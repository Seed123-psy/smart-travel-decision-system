[project]
name = "smart-travel-api"
version = "0.1.0"
description = "Local multi-agent travel planning API"
requires-python = ">=3.11,<3.14"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"

[tool.ruff]
target-version = "py311"
line-length = 100
