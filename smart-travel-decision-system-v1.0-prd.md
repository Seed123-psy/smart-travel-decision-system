# 智能旅行决策系统 - 产品需求文档 (PRD)

**文档版本**：1.1  
**创建时间**：2026-09-02  
**更新时间**：2026-09-09  
**状态**：开发范围已确认；实现、接口验证及验收尚未完成。为保持引用稳定，文件名保留 v1.0。

## 需求描述

### 背景

**业务问题**：旅行规划并非简单“根据目的地生成几天攻略”，一份可落地的行程必须同时满足地点距离、交通时长、景点营业状态、天气、预算、旅行节奏及同行人偏好等多重约束。现有工具存在五大核心痛点：

1. **信息过载与决策疲劳**：攻略碎片化，跨平台比对耗时。
2. **个性化需求难以满足**：通用攻略难以匹配亲子、无障碍、摄影等场景。
3. **行程规划效率低**：景点、餐饮、交通、住宿需要手动衔接。
4. **预算与体验平衡焦虑**：费用构成不透明，隐性消费难以预判。
5. **突发应对不足**：天气突变、景点关闭时缺少替代方案。

**目标用户**：有明确出行意图的个人及群体旅行者。首版由一名操作者统一录入同行人需求，不提供多用户协同编辑。

**价值主张**：让规划更可靠、更可执行、更懂用户；以外部数据和确定性校验支持模型生成的候选方案。

### 首版范围与已确认约束

- **使用场景**：本地运行和演示的 Web 系统；单次规划一个中国大陆目的地城市，1–7 天（起止日期均计入）、1–10 名同行人。
- **技术约束**：Vue 3 + TypeScript + Vite 前端、FastAPI 后端、HelloAgents 多智能体协作、MySQL 8.4 本地数据库。Vite 用于前端开发与构建。具体模块、配置及接口见[开发基线](./development-baseline.md)。
- **规划边界**：规划目的地停留期间的活动、住宿及市内交通；出发地用于需求背景，不承诺安排出发地与目的地之间的往返交通。
- **交付顺序**：先验证模型、地图、天气等真实接口，再完成 P0 闭环；P1 增加预算精算、Critic 及编辑联动。

### 功能概述

| ID | 功能 | 优先级 |
|----|------|--------|
| F001 | 需求收集与结构化（含明确预算） | P0 |
| F002 | 初版行程生成 | P0 |
| F003 | 地图与路线校验 | P0 |
| F004 | 天气与营业校验 | P0 |
| F005 | 确定性预算计算 | P1 |
| F006 | 冲突检测（Critic Agent） | P1 |
| F007 | 行程编辑与联动更新 | P1 |
| F008 | 多人偏好协调 | P2 |
| F009 | 动态重规划 | P2 |
| F010 | 订单/预订接入（远期） | P3 |
| F011 | 决策解释与版本对比（远期增强） | P3 |
| F012 | PDF 行程导出 | P0 |
| F013 | 多 Agent 执行过程与结果展示 | P0 |

F001–F011 保持原编号；F012、F013 为新增的显式交付项。任务、结构化需求、行程、校验结果及 Agent 执行记录的本地持久化属于 P0 公共能力。P0 展示基本依据与风险说明，F011 指高级取舍解释和版本比较。

**功能边界**：

- **P0 包含**：需求输入与预算范围确认 → 多 Agent 资料收集 → 候选行程生成 → 路线、天气和营业校验 → 结果与依据展示 → 保存及 PDF 导出。
- **后续迭代**：P1 预算精算、Critic、行程编辑联动；P2 多人偏好协调、事件触发重规划；P3 预订接入与版本比较。
- **首版不包含**：交易支付、行中实时导航、跨城市串游、多用户账户与协同编辑、原生移动应用、国际旅行。

**用户场景**：

- **P0 个人快速决策**：用户录入“北京出发，杭州 3 天 2 晚，2 人，目的地停留总预算 5000 元，休闲节奏”，确认预算不含往返杭州的大交通费用后，查看 Agent 实际执行状态，获得带地图、校验标记和来源的行程并导出 PDF。
- **P1 预算与编辑**：用户修改酒店或景点，系统重算路线、时间和已知费用，显示未知费用与超支风险。
- **P2 群体及突发场景**：家庭成员配置偏好权重；遇到暴雨或临时闭馆时触发方案调整并说明影响。

### 输入与预算定义

| 字段 | 类型 | 必填 | 规则或默认值 |
|------|------|------|--------------|
| 出发地 origin | 城市 | 是 | 需求背景；允许与目的地相同 |
| 目的地 destination | 城市 | 是 | 单个中国大陆城市 |
| 开始日期 start_date | 日期 | 是 | 不早于 Asia/Shanghai 时区的当天 |
| 结束日期 end_date | 日期 | 是 | 不早于开始日期；总天数为日期差 + 1，范围 1–7 天 |
| 同行人数 travelers | 整数 | 是 | 1–10 人，表示全部实际出行人；若操作者出行则包含其本人，不等同于登录用户数 |
| 总预算 budget_total | 金额 | 是 | 全体同行人在目的地停留期间的总额，CNY，正数，最多两位小数，最大 99999999.99 元 |
| 风格偏好 styles | 枚举数组 | 否 | 默认 []，表示未指定偏好；可选亲子、美食、摄影、自然、人文等 |
| 住宿偏好 hotel_preference | 枚举 | 否 | 默认“不限”，可选酒店档次或民宿偏好 |
| 行程节奏 pace | 枚举 | 否 | 默认“均衡”，可选紧凑、均衡、休闲 |
| 每日活动时段 daily_window | 时间范围 | 否 | 默认 09:00–18:00，同日有效时段 |
| 房间数 rooms | 整数 | 否 | 多日住宿建议 ceil(travelers / 2)，在需求确认页展示并可修改，范围 1–同行人数；当天往返或无住宿需求时为 0 |

**预算口径必须在确认页、结果页与 PDF 中一致展示**：

- 币种固定为人民币 CNY，费用范围固定为 destination_only；总预算是所有同行人合计，不是人均，也不是每日金额。自然语言中的人均预算可按人数换算，换算结果须确认。
- 覆盖门票、住宿、餐饮、市内交通和其他目的地停留支出；不含出发地与目的地之间往返机票、高铁等城际大交通费用。飞机舱位字段从首版输入移除，后续扩展另行定义。
- P0 根据预算选择候选项目，展示预算目标、费用线索及未知项，不提供完整预算精算，不承诺实际总价低于预算，也不验收预算偏差率。
- P1 按单价、数量及计价单位确定性汇总：住宿按每间每晚价格 × 房间数 × 晚数（总天数 − 1）；门票、餐饮等分别声明人数、次数或天数。单日旅行的住宿晚数为 0。
- P1 的 known_total 只累加已验证且有效的价格；estimated_total 为 known_total 加上明确标记且有效的估算价格，不纳入未知或过期费用。估算不等同于已验证报价，只有无未知/过期项时才能展示完整估算金额；否则分别展示有效已知小计、估算明细与缺失项，不给出完整总额保证。
- pricing_status 分为 not_calculated（P0 未精算）、complete（全部价格已验证且有效）、estimated（存在估算但无未知/过期项）、incomplete（存在未知或过期项）。estimated 仅可提示“估算金额”“预计超支”，不能声称“预算校验通过”；只有 complete 才能给出完整预算判断。
- 未知价格必须标记为“待确认”，不能当作 0 元；只有有来源确认免费的项目才记 0 元。incomplete 状态不能声称“预算内”；有效已知小计已超支时，仍应提示至少超支的金额。

**澄清规则**：仅缺少必填字段时追问补齐；非法输入提示修正。可选字段采用上述默认值，在统一确认页展示并允许修改，不因未填写可选字段反复追问。用户确认全部结构化需求及预算口径后才能开始规划。

### 输出与交互

- **P0 输出**：按天、时段排列的景点与餐饮建议；有住宿需求时的住宿候选；地图点位、距离与通勤时间；路线、天气、营业状态及其来源和更新时间；预算目标、费用未知项与“尚未完成费用精算”说明；可恢复查看的本地结果；PDF。
- **P1 输出**：分项预算、超支提示、Critic 报告、行程编辑及联动校验结果。
- **主交互**：Web 对话与结构化表单结合；需求输入 → 必填字段澄清 → 统一确认 → Agent 执行展示 → 行程与校验结果 → 导出。P0 允许重新提交需求生成新方案，局部编辑与联动更新由 F007 提供。
- **Agent 展示**：读取后端实际执行记录，通过轮询展示 queued、running、succeeded、degraded、failed、skipped 状态，以及阶段、耗时、工具名称和结果/证据摘要。无需执行的任务（例如当天往返不查住宿）标记 skipped 并说明原因。不得使用定时动画伪造执行状态或百分比，不展示模型内部推理链。

### 数据需求与失败处理

| 数据依赖 | 用途 | 不可用或超出覆盖范围时 |
|----------|------|--------------------------|
| 地图/POI 与路径服务 | 点位、距离、市内路线及预计通勤时间 | 有可用缓存则注明时间；无法确定路线则标为未验证，不得判定可达 |
| 天气服务 | 目的地预报与室外活动风险 | 标记未知或超出预报期；不得将历史均值或模型预测冒充实时预报 |
| 营业状态来源 | 营业窗口、闭馆及限流信息 | 展示来源不足或待确认状态；模型不能凭空补齐开闭馆事实 |
| 模型服务 | 需求理解、候选建议及行程编排 | 记录失败原因；不能产生可用候选时返回失败状态及重试入口 |
| 价格来源（P1） | 可追溯单价、计价单位与时间 | 记录未知项，不视为免费 |

景点、住宿、天气和营业状态四个专业 Agent 分别获取领域结果。景点、住宿、天气首先并行执行；营业状态查询依赖景点候选 POI，候选产生后再执行，可与尚未完成的其他任务并行。Planner 汇总上述结果，路线与时间计算由确定性服务调用交通适配器执行。普通 API 调用或计算函数不包装为独立 Agent。P1 增加 Critic；预算由确定性服务计算，模型仅辅助解释。

降级需保留缓存标识、来源时间、未知项与失败原因。只有仍可供用户查看且明确标注缺失验证的结果才能作为降级结果；关键依赖缺失、不能形成有依据的行程时应返回失败，不得无条件继续并伪造方案。降级结果不等同于“已验证可执行”。

### 边界情况与分期

1. **天气突变或超出预报期**：P0 标记风险/未知并提示备选方向；自动触发替换与重规划属于 P2。
2. **景点闭馆或限流**：P0 标记来源和风险、给出有依据的备选；不可确认时明确待确认。
3. **路线不可达或跨城误排**：P0 标记问题并修正候选或建议替换，无法修复时不判为通过。
4. **预算不足或费用缺失**：P0 显示预算约束与风险；P1 输出确定性超支计算和节省建议。
5. **同行人偏好冲突**：P0 由操作者统一确认偏好；权重协调与折中方案属于 P2。

## 设计决策与质量约束

### 能力架构

- **专业信息获取与规划分离**：多个真实执行的专业 Agent 提供带来源的领域结果，Planner 生成候选安排。
- **生成与事实校验分离**：路线、到达时间和费用采用确定性服务；天气与营业状态以来源数据为准。
- **规划与评审分期**：P0 完成基础校验；P1 Critic 根据校验结果识别剩余冲突并输出修复建议，不能替代工具验证。
- **本地可追溯**：保存任务、输入、方案、校验结果与 Agent 状态，结果提交数据库成功后才可标记就绪；应用重启后可以重新查看已保存结果，中断的任务标记失败并可重新提交，不要求恢复中断的执行。

### 非功能约束

- **性能与可靠性**：P0 规划耗时 p95 <60 秒、按时段到达率 ≥90%；按下节约定采样验收。P1 才验收预算偏差 <15%。以上均为待验证目标。
- **兼容性**：本地 Web，面向主流桌面浏览器；首版按单操作者、单并发规划验收。
- **数据与配置**：输入只用于规划；外部模型和数据服务仅接收完成调用所需信息。密钥使用本地配置，不写入文档、源码、截图或用户界面。
- **扩展性**：按 P0–P3 分期，保留数据适配器和能力模块边界，不提前实现多用户部署与交易能力。

### 风险与待验证事项

- 多 Agent 输出冲突：P0 保留来源并执行基础校验，P1 增加 Critic。
- 模型、地图、天气、营业数据的实际账号、权限、配额与覆盖率尚未验证；开发首先完成真实调用探针和降级记录。文档确认不代表接口已接通，也不代表指标已达标。
- 接口延迟和数据缺失影响验收：记录真实环境与失败样本，用受控故障验证降级。
- 范围膨胀：P0 不提前纳入预算精算、局部编辑、多人协调或自动重规划。

## 验收标准

### 功能验收

**F001 需求收集与结构化（P0）**

- WHEN 用户提交自然语言或表单需求 THEN 系统 SHALL 形成六项必填字段和可选字段的结构化结果。
- WHEN 必填字段缺失 THEN 系统 SHALL 追问补齐；WHEN 可选字段缺失 THEN 系统 SHALL 展示默认值供确认。
- WHEN 日期、人数、金额或范围非法 THEN 系统 SHALL 提示修正；WHEN 出发地与目的地相同 THEN 系统 SHALL 允许本地游规划。
- WHEN 用户开始规划 THEN 系统 SHALL 已获得对同行总人数、总预算及费用包含/排除范围的确认。

**F002 初版行程生成（P0）**

- WHEN 用户确认有效需求 THEN 系统 SHALL 输出按天、按时段的候选安排及需要住宿时的住宿建议，并受预算目标和活动时段约束。
- WHEN 生成结束 THEN 系统 SHALL 展示结果或明确失败原因，并保存任务、输入、方案及可获得的校验记录。
- WHEN 已保存方案在应用重启后打开 THEN 系统 SHALL 可恢复查看；无需恢复重启前中断的任务执行。

**F003 地图与路线校验（P0）**

- WHEN 行程包含相邻活动点位 THEN 系统 SHALL 基于地图来源校验距离、交通方式、通勤时间和到达窗口。
- WHEN 存在跨城误排、不可达、迟到或缺少路线数据 THEN 系统 SHALL 标记并给出修正方向，不得判为通过。

**F004 天气与营业校验（P0）**

- WHEN 行程含室外项目 THEN 系统 SHALL 展示对应日期的天气风险及数据时间。
- WHEN 景点闭馆或限流 THEN 系统 SHALL 标记风险并推荐有依据的备选。
- WHEN 天气超出预报期或营业来源不足 THEN 系统 SHALL 标为未知/待确认，不补造事实。

**F005 确定性预算计算（P1）**

- WHEN 行程生成或修改 THEN 系统 SHALL 按确认的费用口径、单价、计价单位和数量计算分项金额；金额使用十进制定点数处理。
- WHEN 存在未知或过期费用 THEN 系统 SHALL 标记 pricing_status=incomplete，分别展示待确认项、有效已知小计和估算明细，不以 0 元补齐，也不声称“预算内”。
- WHEN 存在估算且无未知/过期费用 THEN 系统 SHALL 标记 pricing_status=estimated，只展示估算金额和预计超支等提示，不声称“预算校验通过”。
- WHEN 有效已知费用小计已超过预算 THEN 系统 SHALL 提醒至少超支金额并提供节省建议；只有全部价格已验证有效、pricing_status=complete 时才可判定完整总额是否在预算内。

**F006 冲突检测（Critic Agent，P1）**

- WHEN 存在时空、天气、预算或偏好冲突 THEN 系统 SHALL 输出基于工具结果的冲突报告与修复建议。
- WHEN 必要校验均已完成且没有冲突 THEN 系统 SHALL 输出“校验通过”；WHEN 存在未知或未验证项 THEN 系统 SHALL 明确展示，不能判为全部通过。

**F007 行程编辑与联动更新（P1）**

- WHEN 用户修改景点、住宿或时段等元素 THEN 系统 SHALL 重新计算受影响路线、时间和预算。
- WHEN 新校验尚未完成或产生冲突 THEN 系统 SHALL 显示待校验或冲突状态，不沿用旧的通过结论。

**F008 多人偏好协调（P2）**

- WHEN 用户为多名同行人录入不同偏好 THEN 系统 SHALL 支持权重配置并解释折中方案；人数大于 1 本身不触发多用户协同功能。

**F009 动态重规划（P2）**

- WHEN 已接入的天气、营业等事件变化满足触发条件 THEN 系统 SHALL 生成替代安排并重新校验。
- WHEN 重规划完成 THEN 系统 SHALL 说明时间、费用和体验变化及仍存在的未知项。

**F010 订单/预订接入、F011 高级解释与版本对比（P3）**

- WHEN 进入 P3 开发前 THEN 项目 SHALL 分别确认交易边界、授权方式及版本比较的独立验收标准；不作为首版交付要求。

**F012 PDF 行程导出（P0）**

- WHEN 用户导出已生成方案 THEN 系统 SHALL 生成可打开、中文可读的 PDF，包含需求摘要、每日安排、路线摘要、预算口径、风险、未知项及数据来源时间。
- WHEN 方案存在降级或未验证项 THEN 系统 SHALL 在 PDF 中保留相同状态，不将其导出为“已验证通过”。

**F013 多 Agent 执行过程与结果展示（P0）**

- WHEN 专业 Agent 和 Planner 执行 THEN 系统 SHALL 展示与后端记录一致的阶段、状态、耗时、工具名称及结果/证据摘要。
- WHEN Agent 失败、降级或按条件跳过 THEN 系统 SHALL 展示原因和影响；刷新页面后可根据任务记录恢复展示。
- WHEN 演示多个 Agent THEN 系统 SHALL 能核对各自的实际输入、工具调用及领域输出，不能仅通过多个名称或模拟进度冒充协作。

### 指标口径与验证方法

| 指标 | 阶段 | 样本与计算方式 |
|------|------|----------------|
| 规划耗时 p95 <60 秒 | P0 | 固定 20 个有效输入，用真实模型与外部接口、并发 1 逐个记录。从确认后的规划 POST 被后端接收，到可查看的完整或明确降级结果已持久化并就绪；采用 nearest-rank 分位数，20 个样本取排序第 19 个。整体任务截止时间为 60 秒，超时返回原因。失败/超时不得剔除，按未达标样本保留，分位数计算按正无穷计；另报成功、降级、失败与超时数量。 |
| 按时段到达率 ≥90% | P0 | 冻结真实输入测试集，按所有必需的相邻点位行程段计算：有有效来源支撑、预计不晚于下一活动开始且满足游览营业窗口和每日活动时段的段数 / 必需段总数。到达时间按上一活动结束时间 + 交通时间 + 显式缓冲推算，不直接采信 Planner 自填时间。未知、过期、不可达、迟到均计入分母并视为失败；不得只选择可验证的段计算。记录点位、出发/到达时间、停留时长、窗口、交通方式与来源时间。 |
| 预算偏差 <15% | P1 | 固定至少 10 个 pricing_status=complete、全部价格已验证有效且口径一致的参考案例；每例使用 abs(计算总价 − 参考总价) / 参考总价，参考总价须大于 0，所有案例均需小于 15%。对齐人数、房间/晚数、日期及计价单位；含估算、未知或过期价格的行程不参加完整总价指标，并另报排除数量、原因与覆盖情况。 |

验收报告须记录机器配置、模型版本、API 提供方、测试日期、冷启动/预热状态和缓存使用情况；降级结果可以计入响应耗时样本，但不能自动计为可执行验证通过。固定返回值/模拟接口测试用于逻辑回归，与真实接口验收分开报告，不能作为真实数据或性能达标证明。

### 质量与用户验收清单

- [ ] P0：F001–F004、F012、F013 及本地持久化全部通过。
- [ ] 在单日/七日、1 人/10 人、同城游、缺必填字段和可选默认值等边界下正确工作。
- [ ] 对地图、天气、营业来源及模型执行超时/错误进行故障注入，验证缓存、未知、降级与失败原因；关键数据不足时不伪造行程。
- [ ] 真实 Agent 状态与实际执行记录可核对，刷新后可恢复查看任务。
- [ ] P0 两项指标依约完成取样并附原始记录；P1 另行验收预算指标。
- [ ] 预算口径在需求确认、结果和 PDF 一致；未知费用不会显示为免费。
- [ ] PDF 中文、分页和风险标识检查通过；PRD、调研报告与开发基线分期一致。

## 执行阶段

### 阶段 1：开发基线与外部接口验证

**目标**：以本 PRD v1.1 冻结首版范围，验证本地依赖及真实数据来源。

- [ ] P0 六项功能及预算口径评审通过。
- [ ] MySQL 本地连接、模型、地图/POI、天气和营业信息来源完成探针验证，记录覆盖范围、延迟、配额与失败方式。
- **交付物**：开发基线、接口验证记录、固定验收输入集。

### 阶段 2：P0 核心交付

**目标**：完成输入、生成、校验、Agent 展示、保存与导出的本地闭环。

- [ ] F001–F004、F012、F013 及本地持久化验收通过。
- [ ] 规划耗时与可执行率通过真实接口验收；预算仅完成口径确认和候选约束，不验收预算精度。
- **交付物**：可本地运行的最小决策系统、PDF 示例、指标与故障验证报告。

### 阶段 3：P1 增强交付

**目标**：增强预算可信度、冲突评审与行程编辑能力。

- [ ] F005 预算计算、F006 Critic、F007 编辑联动验收通过。
- [ ] 全部价格已验证有效的 complete 参考案例通过预算偏差指标，估算、未知及过期项处理有独立记录。
- **交付物**：具备费用计算、评审和编辑联动的版本及验收报告。

### 阶段 4：P2–P3 演进

**目标**：扩展群体偏好与事件响应能力，再评估远期功能。

- [ ] P2：F008 多人偏好协调、F009 动态重规划按独立测试集验收。
- [ ] P3：F010 预订接入、F011 高级解释与版本对比完成范围评估后再实施。

## 版本记录

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0 | 2026-09-02 | 初版需求与 F001–F011 分期。 |
| 1.1 | 2026-09-09 | 确认本地部署、Vite 及 MySQL 等约束；明确总预算、必填字段与默认值；补充 F012/F013 和持久化；统一 P0/P1 边界、真实 Agent 展示、降级规则及指标取样口径。 |

